import React from 'react'


const Blocks = (props) => {
    return(
        <div>
            <button>
                <p>{props.category}</p>

            </button>
        </div>
    )
}

export default Blocks